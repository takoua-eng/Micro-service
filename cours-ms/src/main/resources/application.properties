# Application name (Eureka service ID / Gateway route target)
spring.application.name=cours-ms

# Config Server Client
spring.cloud.config.enabled=true
spring.config.import=optional:configserver:http://localhost:8887

# Local overrides (Commented out in favor of Config Server)
# server.port=8082
# spring.datasource.url=jdbc:mysql://localhost:3306/cours_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=
